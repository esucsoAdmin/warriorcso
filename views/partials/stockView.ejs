  <!-- Stock Section Begins -->
  <div class="jumbotron">
<script>
  // test for incoming items for output on page.
 (()=>{
   if(items) {
     console.log(items);
   } else {
     console.log('Nothing on items');
     
   }
 })();
</script>
    <div class="row">
      <div class="col">
        <div class="card bg-dark text-white">
          <div class="card-img-overlay">
            <h4 class="card-title">Add Item Form</h4>
            <label class="card-text">Name</label>
          </div>
        </div>
      </div>
        <div class="form-group">
          <form action="stockList">
            <button type="submit" for=""
              class="btn btn-secondary">Show All Items</button>
          </form>
          <form action="addItem">
            <button type="submit" for=""
              class="btn btn-secondary">Add Item</button>
          </form>
          <form action="removeItem">
            <button type="submit" for=""
              class="btn btn-secondary">Remove Item</button>
          </form>
        </div>

      <div class="col header">Stock Items
        <% for(int i = 0; i < item.length; i++) { %>
          <p><%= item.name %></p>
          <% } %>
      </div>
    </div>
  </div>