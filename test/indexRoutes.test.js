const should = require('chai').should();

const app = require('../app');

describe('Index routes tests:', () => {
    it('should get \'/\' and return a 200 status');
    it('should render index.ejs');
    it('should not throw 404 errors');
});
